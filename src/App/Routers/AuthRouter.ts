import express, { Router, Request, Response } from 'express'

const AuthRouter = Router()

// Login route
AuthRouter.post('/login', function (request: Request, response: Response) {
    
})

// Register route
AuthRouter.post('/register', function (request: Request, response: Response) {
    
})

export default AuthRouter
